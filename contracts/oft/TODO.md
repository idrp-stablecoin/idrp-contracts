# TBD

- gating `brodcast` function with onlyOwner or onlyRole or can be called by IDRPController only
- since we have a quorum mechanism, we can do execute `broadcast` while `executeOperation` in IDRPController to follow the same pattern
